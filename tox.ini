[tox]
envlist = py36, py37

[testenv]
passenv = TOXENV CI TRAVIS TRAVIS_*
usedevelop = True
install_command = pip install -U {opts} {packages}
deps = -r{toxinidir}/test-requirements.txt
       -r{toxinidir}/requirements.txt
commands =
   python -V
   py.test -vvv -s --ignore=e2e_test --ignore=frontend
# Patch for https://github.com/travis-ci/travis-ci/issues/7940
setenv =
   BOTO_CONFIG = /dev/null

[testenv:py36-functional]
commands =
   python -V
   {toxinidir}/scripts/kube-init.sh py.test -vvv -s []

[testenv:py37-functional]
commands =
   python -V
   {toxinidir}/scripts/kube-init.sh py.test -vvv -s []

[testenv:coverage]
commands =
   python -V
   {toxinidir}/scripts/kube-init.sh nosetests --with-coverage --cover-package=venus912_dashboard.apis,venus912_dashboard.auth,venus912_dashboard.core,venus912_dashboard.logger,venus912_dashboard.models,venus912_dashboard.utils --cover-tests

[testenv:codecov]
commands =
   codecov
